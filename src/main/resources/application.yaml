spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pharmalot_control
    username: postgres
    password: Opcenter22092025

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081